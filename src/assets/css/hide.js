import { css } from 'styled-components';

export default css`
  clip-path: circle(0);
  visibility: hidden;
  position: absolute;
  width: 1px;
  height: 1px;
  margin: -1px -1px 0 0;
  pointer-events: none;
`;
